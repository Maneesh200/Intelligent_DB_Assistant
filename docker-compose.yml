version: '3.9'

services:
  ai-db-assistant:
    build: .
    container_name: intelligent_db_assistant

    ports:
      - "8501:8501"

    volumes:
      - .:/app

    env_file:
      - .env

    restart: always 